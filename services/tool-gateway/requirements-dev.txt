# Test dependencies for tool-gateway
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Test imports used:
#   pytest          — test runner, pytest.mark.asyncio, pytest.fixture
#   pytest-asyncio  — async test support (asyncio_mode = auto in pytest.ini)
#   respx           — HTTP mock for backend service calls in integration tests
pytest>=7.0
pytest-asyncio>=0.21
respx>=0.20,<1.0

[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short -W ignore::DeprecationWarning
norecursedirs = .git .venv __pycache__ alembic
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
